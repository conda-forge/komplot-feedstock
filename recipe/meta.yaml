{% set name = "komplot" %}
{% set version = "0.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/komplot-{{ version }}.tar.gz
  sha256: 92d846dfe7bea162c7390ae5c95860584556728ec42ab18d89c4a0cb901bcfa5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61
    - wheel
    - setuptools-git-versioning >=2.0,<3
    - matplotlib-base
    - pip
  run:
    - python >={{ python_min }}
    - matplotlib-base
    - numpy

test:
  imports:
    - komplot
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/bwohlberg/komplot
  summary: Simplified matplotlib interface
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - bwohlberg
